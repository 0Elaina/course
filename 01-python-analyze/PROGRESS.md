## 当前位置
第4章 Python可迭代对象 - 4.2 元组（Tuple）

## 已完成模块
| 模块 | 掌握度 |
|------|--------|
| 4.0 可迭代对象、迭代器与数据结构分类 | 独立 |
| 4.1.1 列表的创建与删除 | 独立 |
| 4.1.2 列表元素访问与修改 | 独立 |
| 4.1.3 列表常用方法全解（增/删/查/排/深浅拷贝） | 独立 |
| 4.1.4 列表支持的运算符（+、+=、*、*=、in 与内存机制） | 独立 |
| 4.1.5 适用于列表的内置函数与高阶函数（sorted/zip/enumerate等） | 独立 |
| 4.1.6 列表推导式语法与核心应用（过滤与三元分支） | 独立 |
| 4.1.7 切片操作全解（越界宽容与原地切片赋值） | 独立 |

## 薄弱点
| 知识点 | 问题描述 |
|--------|---------|
| range 与普通迭代器的操作边界 | 需谨记 range 支持下标/len但不支持 next()；迭代器单向惰性取值，不支持下标访问 |
| 列表推导式 if 位置规则 | 过滤删减放末尾无 else；二选一映射三元放开头必须带 else（已变式题通关巩固） |
